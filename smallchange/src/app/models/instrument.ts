export class Instrument{

    constructor(
        instrumentId: string,
        instrumentDescription: string,
        externalIdType: string,
        externalId: string,
        categoryId: string,
        minQuantity: number,
        maxQuantity: number
    ){

    }
}
